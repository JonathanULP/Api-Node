const { request, response } = require('express');
const jwt = require('jsonwebtoken');



const destruirJWT = (uid = '') => {


    return new Promise( ( resolve , reject ) => {

        const { uid } = req.usuario;

    });

}